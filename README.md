## Converts Number to binary representation, using ESP32, 74HC595, IR sensor module, OLED display.
## It works, however there are some functions that need to be added, so the project doesn't finished yet.
